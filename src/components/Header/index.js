import { Box } from '@mui/material';
import Content from './Content';

const Header = () => {
    return (
        <Box marginTop={8}>
            <Content />
        </Box>
    );
}

export default Header;
